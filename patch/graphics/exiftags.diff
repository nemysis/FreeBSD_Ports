===> Generating patch
===> Viewing diff with more
diff -ruN --exclude=CVS /usr/ports/graphics/exiftags/Makefile ./Makefile
--- /usr/ports/graphics/exiftags/Makefile	2013-01-14 17:57:03.000000000 +0100
+++ ./Makefile	2013-03-29 02:39:27.000000000 +0100
@@ -1,10 +1,5 @@
-# ex:ts=8
-# Ports collection makefile for:	exiftags
-# Date created:			Sep 13, 2002
-# Whom:				ijliao
-#
+# Created by: ijliao
 # $FreeBSD: head/graphics/exiftags/Makefile 300896 2012-07-14 13:54:48Z beat $
-#
 
 PORTNAME=	exiftags
 PORTVERSION=	1.01
@@ -12,7 +7,7 @@
 MASTER_SITES=	http://johnst.org/sw/exiftags/
 
 MAINTAINER=	multimedia@FreeBSD.org
-COMMENT=	Parses a specified JPEG file for a JPEG APP1 section containing Exif data
+COMMENT=	Displaying and modifying Exif tags in digital camera JPEG files
 
 MAKE_ARGS=	CC="${CC}" CFLAGS="${CFLAGS}" PREFIX="${PREFIX}"
 
@@ -20,16 +15,17 @@
 PORTDOCS=	CHANGES README
 PLIST_FILES=	bin/exifcom bin/exiftags bin/exiftime
 
+.include <bsd.port.options.mk>
+
 do-install:
 .for i in exifcom exiftags exiftime
 	${INSTALL_PROGRAM} ${WRKSRC}/${i} ${PREFIX}/bin
-	${INSTALL_MAN} ${WRKSRC}/${i}.1 ${MANPREFIX}/man/man1
+	${INSTALL_MAN} ${WRKSRC}/${i}.1 ${MAN1PREFIX}/man/man1
 .endfor
-.if !defined(NOPORTDOCS)
+
+.if ${PORT_OPTIONS:MDOCS}
 	@${MKDIR} ${DOCSDIR}
-.for i in CHANGES README
-	${INSTALL_DATA} ${WRKSRC}/${i} ${DOCSDIR}
-.endfor
+	${INSTALL_DATA} ${PORTDOCS:S|^|${WRKSRC}/|} ${DOCSDIR}
 .endif
 
 .include <bsd.port.mk>
===> Done
