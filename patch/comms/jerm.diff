===> Generating patch
===> Viewing diff with more
diff -ruN --exclude=CVS /usr/ports/comms/jerm/Makefile ./Makefile
--- /usr/ports/comms/jerm/Makefile	2013-01-14 17:54:33.000000000 +0100
+++ ./Makefile	2013-04-03 13:43:52.000000000 +0200
@@ -32,6 +32,8 @@
 post-patch:
 	@${REINPLACE_CMD} -e 's|^CFLAGS=|CFLAGS?=|' \
 		${WRKSRC}/Makefile
+	@${REINPLACE_CMD} -e 's|sys/termios.h|termios.h|' \
+		${WRKSRC}/tiocdtr.c
 
 do-install:
 	${INSTALL_PROGRAM} ${WRKSRC}/jerm ${PREFIX}/bin
@@ -42,9 +44,9 @@
 	${INSTALL_SCRIPT} ${WRKSRC}/init.sh ${PREFIX}/libexec/jerm
 	${INSTALL_SCRIPT} ${WRKSRC}/rock.pl ${PREFIX}/libexec/jerm
 .endif
-.if ${PORT_OPTIONS:MOCS}
+.if ${PORT_OPTIONS:MDOCS}
 	@${MKDIR} ${DOCSDIR}
-	${INSTALL_DATA} ${WRKSRC}/README ${DOCSDIR}
+	${INSTALL_DATA} ${PORTDOCS:S|^|${WRKSRC}/|} ${DOCSDIR}
 .endif
 
 .include <bsd.port.mk>
===> Done
