# Created by: nemysis <nemysis@gmx.ch>
# $FreeBSD$

PORTNAME=	mobile-broadband-provider-info
PORTVERSION=	20130312
CATEGORIES=	net
MASTER_SITES=	http://archive.ubuntu.com/ubuntu/pool/main/m/${PORTNAME}/
DISTNAME=	${PORTNAME}_${DISTVERSION}.orig

MAINTAINER=	nemysis@gmx.ch
COMMENT=	Mobile broadband settings for different service provider

WRKSRC=		${WRKDIR}/${PORTNAME}-${PORTVERSION}

BUILD_DEPENDS=	gawk:${PORTSDIR}/lang/gawk

GNU_CONFIGURE=	yes

PORTDOCS=	MAINTAINERS NEWS README README.svn

.include <bsd.port.options.mk>

post-patch:
	@${REINPLACE_CMD} -e 's|$$(datadir)/pkgconfig|$$(prefix)/libdata/pkgconfig|' \
		${WRKSRC}/Makefile.in

post-install:
.if ${PORT_OPTIONS:MDOCS}
	${MKDIR} ${DOCSDIR}
	${INSTALL_DATA} ${PORTDOCS:S|^|${WRKSRC}/|} ${DOCSDIR}
.endif

.include <bsd.port.mk>
