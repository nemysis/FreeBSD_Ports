===> Generating patch
===> Viewing diff with more
diff -ruN --exclude=CVS /usr/ports/graphics/cbviewer/Makefile ./Makefile
--- /usr/ports/graphics/cbviewer/Makefile	2013-01-24 11:58:04.000000000 +0100
+++ ./Makefile	2013-05-27 23:54:03.000000000 +0200
@@ -1,4 +1,4 @@
-# Created by: nemysis@gmx.ch
+# Created by: nemysis <nemysis@gmx.ch>
 # $FreeBSD: head/graphics/cbviewer/Makefile 310871 2013-01-23 15:00:18Z makc $
 
 PORTNAME=	cbviewer
@@ -37,8 +37,7 @@
 
 SUB_FILES=	${PORTNAME}
 
-DESKTOP_ENTRIES="CBViewer" "Comic book viewer" \
-		"${PREFIX}/share/pixmaps/${PORTNAME}.png" \
+DESKTOP_ENTRIES="CBViewer" "${COMMENT}" "${PORTNAME}" \
 		"${PORTNAME}" "Graphics;Viewer;Java;" true
 
 .include <bsd.port.options.mk>
@@ -52,7 +51,7 @@
 	${INSTALL_DATA} ${_DISTDIR}/${PORTNAME}.png ${PREFIX}/share/pixmaps
 
 .if ${PORT_OPTIONS:MDOCS}
-	${MKDIR} ${DOCSDIR}
+	@${MKDIR} ${DOCSDIR}
 	${INSTALL_DATA} ${PORTDOCS:S|^|${WRKSRC}/|} ${DOCSDIR}
 .endif
 
===> Done
