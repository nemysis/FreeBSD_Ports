===> Generating patch
===> Viewing diff with more
diff -ruN --exclude=CVS /usr/ports/graphics/batik/Makefile ./Makefile
--- /usr/ports/graphics/batik/Makefile	2013-01-14 17:56:59.000000000 +0100
+++ ./Makefile	2013-05-26 20:45:49.000000000 +0200
@@ -14,7 +14,7 @@
 LICENSE=	AL2
 
 USE_ZIP=	yes
-USE_JAVA=	1.5+
+USE_JAVA=	1.6+
 USE_ANT=	yes
 ALL_TARGET=	jars all-jar
 # Prevent out of memory error during build
@@ -29,11 +29,11 @@
 .include <bsd.port.options.mk>
 
 do-install:
-	cd ${INSTALL_WRKSRC} && ${COPYTREE_SHARE} . ${DATADIR}
+	@(cd ${INSTALL_WRKSRC} && ${COPYTREE_SHARE} . ${DATADIR})
 
 post-install:
 .if ${PORT_OPTIONS:MDOCS}
-	${MKDIR} ${DOCSDIR}
+	@${MKDIR} ${DOCSDIR}
 	${INSTALL_DATA} ${PORTDOCS:S|^|${WRKSRC}/|} ${DOCSDIR}
 .endif
 
===> Done
