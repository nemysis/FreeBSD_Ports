===> Generating patch
===> Viewing diff with more
diff -ruN --exclude=CVS /usr/ports/games/xgalaga++/Makefile ./Makefile
--- /usr/ports/games/xgalaga++/Makefile	2013-01-24 11:57:58.000000000 +0100
+++ ./Makefile	2013-03-28 19:19:28.000000000 +0100
@@ -1,3 +1,4 @@
+# Created by: nemysis <nemysis@gmx.ch>
 # $FreeBSD: head/games/xgalaga++/Makefile 310871 2013-01-23 15:00:18Z makc $
 
 PORTNAME=	xgalaga++
@@ -31,23 +32,22 @@
 PORTDOCS=	README
 
 DESKTOP_ENTRIES="XGalaga++" "${COMMENT}" "${PORTNAME}" \
-		"${PORTNAME}" "Game;ArcadeGame;" ${FALSE}
+		"${PORTNAME}" "Game;ArcadeGame;" false
 
 .include <bsd.port.options.mk>
 
 post-patch:
-	@${REINPLACE_CMD} -e '/^CXX =/ d; s|/usr/local|${LOCALBASE}|' \
-		${WRKSRC}/Makefile
+	@${REINPLACE_CMD} -e '/^CXX =/ d' ${WRKSRC}/Makefile
 
 do-install:
 	${INSTALL_PROGRAM} ${WRKSRC}/${PORTNAME} ${PREFIX}/bin
 	${INSTALL_MAN} ${WRKSRC}/${MAN6}x ${MAN6PREFIX}/man/man6/${MAN6}
 	${CHGRP} games ${PREFIX}/bin/${PORTNAME}
 	${CHMOD} g+s  ${PREFIX}/bin/${PORTNAME}
-	${INSTALL_DATA} ${DISTDIR}/${DIST_SUBDIR}/${PORTNAME}.png ${PREFIX}/share/pixmaps
+	${INSTALL_DATA} ${_DISTDIR}/${PORTNAME}.png ${PREFIX}/share/pixmaps
 
 .if ${PORT_OPTIONS:MDOCS}
-	${MKDIR} ${DOCSDIR}
+	@${MKDIR} ${DOCSDIR}
 	${INSTALL_DATA} ${PORTDOCS:S|^|${WRKSRC}/|} ${DOCSDIR}
 .endif
 
===> Done
