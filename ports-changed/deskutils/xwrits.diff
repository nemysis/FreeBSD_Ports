===> Generating patch
===> Viewing diff with more
diff -ruN --exclude=CVS /usr/ports/deskutils/xwrits/Makefile ./Makefile
--- /usr/ports/deskutils/xwrits/Makefile	2013-01-14 17:54:27.000000000 +0100
+++ ./Makefile	2013-05-25 14:09:14.000000000 +0200
@@ -9,6 +9,8 @@
 MAINTAINER=	nemysis@gmx.ch
 COMMENT=	Reminds you to take wrist breaks
 
+LICENSE=	GPLv2
+
 GNU_CONFIGURE=	yes
 
 USE_XORG=	ice sm x11 xau xdmcp xext xinerama
@@ -23,7 +25,7 @@
 
 post-install:
 .if ${PORT_OPTIONS:MDOCS}
-	${MKDIR} ${DOCSDIR}
+	@${MKDIR} ${DOCSDIR}
 	${INSTALL_DATA} ${PORTDOCS:S|^|${WRKSRC}/|} ${DOCSDIR}
 .endif
 
===> Done
